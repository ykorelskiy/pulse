editorial:
  tone: "satirical_direct"
  # Note: Per explicit user directive, mandatory allegory filtering and politician face bans are disabled.
  # Content is sent to the author brief as-is with real names and key news phrases.
  enforce_allegories: false
  forbidden_topics: []
